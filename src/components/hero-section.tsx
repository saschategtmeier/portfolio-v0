import { FC } from 'react';
import { ContentSection } from './content-section';

export const HeroSection: FC = () => {
  return (
    <ContentSection id='hero' className='bg-green-200'>
      hero
    </ContentSection>
  );
};
